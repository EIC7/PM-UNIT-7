# State file dari scripts/notify-ra-status-poll.js (checkAndAlertSupabaseHealth) --
# dipersist lintas run GitHub Actions lewat actions/cache, bukan lewat git.
# Muncul di working directory kalau script itu dijalankan manual/lokal.
.health-state.json

# File referensi mentah yang user taruh langsung di folder repo (BUKAN
# lewat upload chat) sebagai contoh format JSA terbaru -- berisi tanda
# tangan ASLI (real, belum disanitasi) milik orang sungguhan. Versi
# bersihnya sudah diekstrak jadi jsa_template.docx (root repo, sudah
# disanitasi -- lihat CLAUDE.md). Repo ini PUBLIC, jangan pernah commit
# folder ini apa adanya.
/JSA/

# Full pg_dump database production (dibuat 2026-09-04 untuk migrasi
# backend Supabase, lihat CLAUDE.md bagian "Migrasi backend Supabase") --
# 20MB+, berisi SELURUH data mentah pm_records/outage_assets. Repo ini
# PUBLIC, jangan pernah commit dump database apa pun.
backup_full.sql

# Backup koleksi Firestore project pomi-checksheet-e7 (approvals/checksheets
# -- backend Review Approval Dashboard, repo terpisah), dibuat 2026-09-04
# lewat REST API publik sebagai cadangan data, BUKAN migrasi (dashboard
# tetap pakai project Firebase yang sama). Berisi data mentah submission
# (nama PIC, WO, dst). Repo ini PUBLIC, jangan pernah commit.
firebase_backup_approvals.json
firebase_backup_checksheets.json
